{
  "name": "strftime",
  "version": "0.5.4",
  "description": "lightweight ruby-like strftime date formatting implementation",
  "repository": "https://github.com/sharon-xa/strftime.git",
  "author": "sharon-xa",
  "scripts": {
    "compile": "rm -rf dist/lib && tsc && tsc --build tsconfig.es5.json",
    "test": "jest"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "ts-loader": "^9.5.1",
    "typescript": "^5.5.2"
  }
}
